const loginRouter = require('./login.router');
const userRouter = require('./user.router');
const categorieRouter = require('./category.router');
const postRouter = require('./post.router');

module.exports = {
  loginRouter,
  userRouter,
  categorieRouter,
  postRouter,
};
